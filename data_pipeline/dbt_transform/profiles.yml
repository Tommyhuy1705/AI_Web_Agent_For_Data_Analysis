omni_revenue:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('SUPABASE_DB_HOST', 'aws-0-ap-southeast-1.pooler.supabase.com') }}"
      port: "{{ env_var('SUPABASE_DB_PORT', '6543') | int }}"
      user: "{{ env_var('SUPABASE_DB_USER', 'postgres.qunhqqnmsqiahsrneqcy') }}"
      password: "{{ env_var('SUPABASE_DB_PASSWORD', '') }}"
      dbname: "{{ env_var('SUPABASE_DB_NAME', 'postgres') }}"
      schema: analytics_mart
      threads: 4
